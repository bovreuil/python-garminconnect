# Flask Configuration
SECRET_KEY=your-secret-key-here-change-this-in-production
FLASK_ENV=development

# Database Configuration (SQLite for both local and production)
DATABASE_URL=sqlite:///garmin_hr.db

# Google OAuth Configuration (required for authentication)
GOOGLE_ID=your-google-oauth-client-id
GOOGLE_SECRET=your-google-oauth-client-secret

# Encryption Key (generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your-encryption-key-here

# Heroku Configuration (if deploying to Heroku)
# DATABASE_URL will be automatically set by Heroku Postgres addon 